<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user, size: 52 %>
</a>
<h1>
	<%= current_user.name %>
</h1>
<span>
	<%= link_to "プロフィール", current_user %>
</span>
<span>
	<p>投稿記事：<%= current_user.microposts.count %>件</p>
</span>